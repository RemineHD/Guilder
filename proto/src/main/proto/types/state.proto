syntax = "proto3";

package Types;

option java_package = "dev.remine.guilder.rpc.types";
option java_multiple_files = true;

enum State
{
  CRAFTED = 0;
  RUNNING = 1;
  STOPPED = 2;
  DELETED = 3;
}